#!/usr/bin/env python3
"""ar724 — main CLI entrypoint.

Dispatches to ar724.cli.main. Kept as a thin wrapper so the bin/ares name
matches the operator docs (PRD §22.4) and so callers can `python -m ar724`
or `ares ...` interchangeably.
"""
import sys
from ar724.cli import main

if __name__ == "__main__":
    sys.exit(main())
